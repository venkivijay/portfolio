# Everything is open to everyone, including AI crawlers and assistants
# (GPTBot, ClaudeBot, PerplexityBot, Google-Extended, Applebot-Extended and
# the rest) — reach is the point.
#
# They were previously listed in their own groups, which was a no-op beside
# this one and an active trap: per RFC 9309 a crawler that matches a specific
# group ignores `*` entirely, so any future Disallow added here would not
# have applied to them.
User-agent: *
Allow: /

Sitemap: https://venkivijay.com/sitemap.xml
